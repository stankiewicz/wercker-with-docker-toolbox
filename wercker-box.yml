name: ubuntu14.04 dockered
version: 0.1.1
description: Ubuntu 14.04
type: main
platform: ubuntu@12.04
no-response-timeout: 10
script: |
  sudo apt-get update
  sudo apt-get install docker-engine
  sudo service docker start
  sudo docker run hello-world
  docker version
  docker info
